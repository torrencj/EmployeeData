

<tr>
  <td>"name"</td>
  <td>"role"</td>
  <td>"start"</td>
  <td>"sometext"</td>
</td>

// Function to generate a new table row with passed-in data.
function addRow(name, role, start, role) {

  var newRow = $("<tr>").append( $("<td>").text("name") )
                        .append( $("<td>").text("role") )
                        .append( $("<td>").text("start") )
                        .append( $("<td>").text("rate") )
}
